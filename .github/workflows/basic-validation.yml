name: Basic validation

on:
  push:
    branches:
      - main
      - releases/*
    paths-ignore:
      - '**.md'
  pull_request:

jobs:
  call-basic-validation:
    name: Basic validation
    uses: chiranjib-swain/reusable-workflows/.github/workflows/basic-validation.yml@clear-audit-annotations
    with:
      node-version: '24.x'
